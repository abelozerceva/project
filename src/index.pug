html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Document
        link(rel="stylesheet", href="style.css")
    body
        header
            .logo logo
            .menu
                - for (i = 1; i < 6; i++)
                    .block(class=`block${i}`) text

        .container
            .elements
                -for (i = 1; i < 31; i++)
                    .elem(class=`elem${i}`) 


        script(src='./main.js')